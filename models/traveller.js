const Traveller = function(journeys) {
  this.journeys = journeys;
};

Traveller.prototype.getJourneyStartLocations = function() {
  //GOAL: I want to return an array of all the startLocations
  // create a new variable x
  // the value of the variable will be an arrow function that uses a map method that creates an array of the same length as "journeys" 
  // return "journey.startLocation"
  // return variable x outside of the loop in order to return the desired array.

};

Traveller.prototype.getJourneyEndLocations = function () {

};

Traveller.prototype.getJourneysByTransport = function (transport) {

};

Traveller.prototype.getJourneysByMinDistance = function (minDistance) {

};

Traveller.prototype.calculateTotalDistanceTravelled = function () {

};

Traveller.prototype.getUniqueModesOfTransport = function () {

};


module.exports = Traveller;
